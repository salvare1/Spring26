version: 2.1

jobs:
  test:
    docker:
      - image: cimg/python:3.10
    steps:
      - checkout
      - run: pip install -r requirements.txt || true
      - run: python -m unittest discover

workflows:
  version: 2
  test-workflow:
    jobs:
      - test
